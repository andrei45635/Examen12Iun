package org.example.repo.winners;

import org.example.User;
import org.example.Winner;
import org.example.repo.IRepository;

public interface WinnersRepository extends IRepository<Integer, Winner> {
}
